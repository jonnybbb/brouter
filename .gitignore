*.iml
.gradle
.idea/*
build
/local.properties
/.idea/caches
/.idea/gradle.xml
/.idea/libraries
/.idea/modules.xml
/.idea/workspace.xml
/.idea/navEditor.xml
/.idea/assetWizardSettings.xml
.DS_Store
/captures
.externalNativeBuild
.cxx
misc/segments4
/segments4
# Golden baseline GeoJSON tracks (large, regenerated from test runs)
*_track.geojson
misc/customprofiles
*.BAK
.classpath
.project
!/.idea/runConfigurations
!/.idea/inspectionProfiles
!/.idea/inspectionProfiles/*

# Node tooling under tools/ (analysis scripts) — installed packages, not source
node_modules/

# Local GPX corpus and other working data for the loop-quality harness
tmp/

# GpxLoopSimilarityTest live output — versioned baseline snapshots use the
# `*-baseline.{json,md}` naming convention; the per-run `*-loop-similarity.*`
# files are overwritten by every test invocation and are not tracked.
docs/features/gpx-loop-similarity/*-loop-similarity.json
docs/features/gpx-loop-similarity/*-loop-similarity.md

# Investigation notes + baseline snapshots — kept locally on the fork's
# master for personal reference, not committed (removed from PR #903 via
# commit fc3a2db0 to avoid shipping work-in-progress notes upstream).
docs/features/roundtrip-closure-aware-baseline.json
docs/features/roundtrip-closure-aware-control-node-planning.md
docs/features/roundtrip-benchmark-2026-05.md
docs/features/gpx-loop-similarity/Basel-*-baseline.json
docs/features/gpx-loop-similarity/Basel-*-baseline.md

# Local-only "known failure" investigation tests and analysis tooling — kept on
# disk for manual debugging (opt-in via -Dloop.tests=true) but not part of the PR.
brouter-core/src/test/java/btools/router/known/
tools/visualize-failures.py
