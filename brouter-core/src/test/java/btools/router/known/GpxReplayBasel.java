package btools.router.known;

import java.io.File;
import java.util.ArrayList;
import java.util.List;

import btools.router.OsmNodeNamed;
import btools.router.OsmTrack;
import btools.router.RoundTripQualityGate;
import btools.router.RoundTripQualityGate.HostileStretch;
import btools.router.RoutingContext;
import btools.router.RoutingEngine;

import org.junit.Assert;
import org.junit.Assume;
import org.junit.Test;

/**
 * GPX replay tests — sample waypoints from known-good cycling routes
 * and feed them to brouter as point-to-point waypoints (not round-trip).
 * Isolates the routing engine from the round-trip planner:
 *   - PASS means brouter's fastbike profile and routing engine can
 *     reproduce the route's road choices given the right waypoints;
 *     the round-trip planner's candidate placement is the bottleneck.
 *   - FAIL means brouter's engine traverses hostile content even when
 *     told to go through good waypoints; the fastbike profile
 *     cost-function is the bottleneck.
 *
 * Generated by {@code tools/replay-gpx-via-brouter.mjs}.
 */
public class GpxReplayBasel {

  private File profileFile(String name) {
    File p = new File("misc/profiles2/" + name + ".brf");
    if (!p.exists()) p = new File("../misc/profiles2/" + name + ".brf");
    return p.exists() ? p : null;
  }

  /**
   * Replay of "2022-08-05_873112310_ALRD 58 Hilly GER Wiechs" — feeds the
   * sampled waypoints to brouter and reports whether the resulting
   * point-to-point route is clean enough to satisfy the round-trip
   * quality gate. Failure means the routing engine (with the
   * fastbike profile) can't reproduce the known-good route
   * even when given the right waypoints.
   */
  @Test
  public void replay_2022_08_05_873112310_ALRD_58_Hilly_GER_Wiechs() throws Exception {
    Assume.assumeTrue("loop-tests opt-in", Boolean.getBoolean("loop.tests"));
    File segDir = new File("../segments4");
    if (!segDir.exists()) segDir = new File("segments4");
    File segFile = new File(segDir, "E5_N45.rd5");
    Assume.assumeTrue("segment present at " + segFile.getAbsolutePath(), segFile.exists());
    File pf = profileFile("fastbike");
    Assume.assumeTrue("profile present", pf != null);

    List<OsmNodeNamed> wplist = new ArrayList<>();
    { OsmNodeNamed n = new OsmNodeNamed(); n.name = "from"; n.ilon = 187591524; n.ilat = 137562900; wplist.add(n); }
    { OsmNodeNamed n = new OsmNodeNamed(); n.name = "via1"; n.ilon = 187665292; n.ilat = 137571053; wplist.add(n); }
    { OsmNodeNamed n = new OsmNodeNamed(); n.name = "via2"; n.ilon = 187716822; n.ilat = 137610179; wplist.add(n); }
    { OsmNodeNamed n = new OsmNodeNamed(); n.name = "via3"; n.ilon = 187796368; n.ilat = 137592802; wplist.add(n); }
    { OsmNodeNamed n = new OsmNodeNamed(); n.name = "via4"; n.ilon = 187776764; n.ilat = 137640349; wplist.add(n); }
    { OsmNodeNamed n = new OsmNodeNamed(); n.name = "to"; n.ilon = 187652450; n.ilat = 137620516; wplist.add(n); }

    RoutingContext rctx = new RoutingContext();
    rctx.localFunction = pf.getAbsolutePath();
    RoutingEngine re = new RoutingEngine(null, null, segDir, wplist, rctx);
    re.quite = true;
    re.doRun(0);

    String err = re.getErrorMessage();
    OsmTrack track = re.getFoundTrack();
    System.out.println("=== replay_2022_08_05_873112310_ALRD_58_Hilly_GER_Wiechs ===");
    System.out.println("  error: " + (err == null ? "PASSED" : err));
    if (track != null && track.nodes != null) {
      System.out.println("  route: " + track.distance + "m / " + track.nodes.size() + " nodes");
      HostileStretch hs = RoundTripQualityGate.worstHostileStretchPaved(track);
      if (hs.isPresent()) {
        System.out.printf("  worst-hostile: %dm at (%.6f, %.6f) tags: %s%n",
          hs.meters, hs.startLat(), hs.startLon(), hs.startTags);
      } else {
        System.out.println("  hostile: none");
      }
      Assert.assertNull("expected clean point-to-point route through Komoot waypoints: " + err, err);
      Assert.assertTrue("expected no hostile stretch over the gate cap",
        !hs.isPresent() || hs.meters <= RoundTripQualityGate.MAX_CONTIGUOUS_HOSTILE_METERS);
    } else {
      Assert.fail("brouter returned no track: " + err);
    }
  }

  /**
   * Replay of "dayout__1052778076__#18 DE RONDE 2023" — feeds the
   * sampled waypoints to brouter and reports whether the resulting
   * point-to-point route is clean enough to satisfy the round-trip
   * quality gate. Failure means the routing engine (with the
   * fastbike profile) can't reproduce the known-good route
   * even when given the right waypoints.
   */
  @Test
  public void replay_dayout_1052778076_18_DE_RONDE_2023() throws Exception {
    Assume.assumeTrue("loop-tests opt-in", Boolean.getBoolean("loop.tests"));
    File segDir = new File("../segments4");
    if (!segDir.exists()) segDir = new File("segments4");
    File segFile = new File(segDir, "E5_N45.rd5");
    Assume.assumeTrue("segment present at " + segFile.getAbsolutePath(), segFile.exists());
    File pf = profileFile("fastbike");
    Assume.assumeTrue("profile present", pf != null);

    List<OsmNodeNamed> wplist = new ArrayList<>();
    { OsmNodeNamed n = new OsmNodeNamed(); n.name = "from"; n.ilon = 187591524; n.ilat = 137562900; wplist.add(n); }
    { OsmNodeNamed n = new OsmNodeNamed(); n.name = "via1"; n.ilon = 187653169; n.ilat = 137505569; wplist.add(n); }
    { OsmNodeNamed n = new OsmNodeNamed(); n.name = "via2"; n.ilon = 187771317; n.ilat = 137572850; wplist.add(n); }
    { OsmNodeNamed n = new OsmNodeNamed(); n.name = "via3"; n.ilon = 187766788; n.ilat = 137689772; wplist.add(n); }
    { OsmNodeNamed n = new OsmNodeNamed(); n.name = "via4"; n.ilon = 187719403; n.ilat = 137702802; wplist.add(n); }
    { OsmNodeNamed n = new OsmNodeNamed(); n.name = "to"; n.ilon = 187641065; n.ilat = 137643319; wplist.add(n); }

    RoutingContext rctx = new RoutingContext();
    rctx.localFunction = pf.getAbsolutePath();
    RoutingEngine re = new RoutingEngine(null, null, segDir, wplist, rctx);
    re.quite = true;
    re.doRun(0);

    String err = re.getErrorMessage();
    OsmTrack track = re.getFoundTrack();
    System.out.println("=== replay_dayout_1052778076_18_DE_RONDE_2023 ===");
    System.out.println("  error: " + (err == null ? "PASSED" : err));
    if (track != null && track.nodes != null) {
      System.out.println("  route: " + track.distance + "m / " + track.nodes.size() + " nodes");
      HostileStretch hs = RoundTripQualityGate.worstHostileStretchPaved(track);
      if (hs.isPresent()) {
        System.out.printf("  worst-hostile: %dm at (%.6f, %.6f) tags: %s%n",
          hs.meters, hs.startLat(), hs.startLon(), hs.startTags);
      } else {
        System.out.println("  hostile: none");
      }
      Assert.assertNull("expected clean point-to-point route through Komoot waypoints: " + err, err);
      Assert.assertTrue("expected no hostile stretch over the gate cap",
        !hs.isPresent() || hs.meters <= RoundTripQualityGate.MAX_CONTIGUOUS_HOSTILE_METERS);
    } else {
      Assert.fail("brouter returned no track: " + err);
    }
  }

  /**
   * Replay of "dayout__1309530502__#21 ROTFL 2023" — feeds the
   * sampled waypoints to brouter and reports whether the resulting
   * point-to-point route is clean enough to satisfy the round-trip
   * quality gate. Failure means the routing engine (with the
   * fastbike profile) can't reproduce the known-good route
   * even when given the right waypoints.
   */
  @Test
  public void replay_dayout_1309530502_21_ROTFL_2023() throws Exception {
    Assume.assumeTrue("loop-tests opt-in", Boolean.getBoolean("loop.tests"));
    File segDir = new File("../segments4");
    if (!segDir.exists()) segDir = new File("segments4");
    File segFile = new File(segDir, "E5_N45.rd5");
    Assume.assumeTrue("segment present at " + segFile.getAbsolutePath(), segFile.exists());
    File pf = profileFile("fastbike");
    Assume.assumeTrue("profile present", pf != null);

    List<OsmNodeNamed> wplist = new ArrayList<>();
    { OsmNodeNamed n = new OsmNodeNamed(); n.name = "from"; n.ilon = 187574109; n.ilat = 137574777; wplist.add(n); }
    { OsmNodeNamed n = new OsmNodeNamed(); n.name = "via1"; n.ilon = 187490788; n.ilat = 137473650; wplist.add(n); }
    { OsmNodeNamed n = new OsmNodeNamed(); n.name = "via2"; n.ilon = 187312991; n.ilat = 137438518; wplist.add(n); }
    { OsmNodeNamed n = new OsmNodeNamed(); n.name = "via3"; n.ilon = 187367350; n.ilat = 137369621; wplist.add(n); }
    { OsmNodeNamed n = new OsmNodeNamed(); n.name = "via4"; n.ilon = 187553006; n.ilat = 137355306; wplist.add(n); }
    { OsmNodeNamed n = new OsmNodeNamed(); n.name = "to"; n.ilon = 187590536; n.ilat = 137440411; wplist.add(n); }

    RoutingContext rctx = new RoutingContext();
    rctx.localFunction = pf.getAbsolutePath();
    RoutingEngine re = new RoutingEngine(null, null, segDir, wplist, rctx);
    re.quite = true;
    re.doRun(0);

    String err = re.getErrorMessage();
    OsmTrack track = re.getFoundTrack();
    System.out.println("=== replay_dayout_1309530502_21_ROTFL_2023 ===");
    System.out.println("  error: " + (err == null ? "PASSED" : err));
    if (track != null && track.nodes != null) {
      System.out.println("  route: " + track.distance + "m / " + track.nodes.size() + " nodes");
      HostileStretch hs = RoundTripQualityGate.worstHostileStretchPaved(track);
      if (hs.isPresent()) {
        System.out.printf("  worst-hostile: %dm at (%.6f, %.6f) tags: %s%n",
          hs.meters, hs.startLat(), hs.startLon(), hs.startTags);
      } else {
        System.out.println("  hostile: none");
      }
      Assert.assertNull("expected clean point-to-point route through Komoot waypoints: " + err, err);
      Assert.assertTrue("expected no hostile stretch over the gate cap",
        !hs.isPresent() || hs.meters <= RoundTripQualityGate.MAX_CONTIGUOUS_HOSTILE_METERS);
    } else {
      Assert.fail("brouter returned no track: " + err);
    }
  }

  /**
   * Replay of "dayout__1365663830__#10 GRVL 106 mountainous SUI Bretzwil" — feeds the
   * sampled waypoints to brouter and reports whether the resulting
   * point-to-point route is clean enough to satisfy the round-trip
   * quality gate. Failure means the routing engine (with the
   * fastbike profile) can't reproduce the known-good route
   * even when given the right waypoints.
   */
  @Test
  public void replay_dayout_1365663830_10_GRVL_106_mountainous_SUI_Bretzwil() throws Exception {
    Assume.assumeTrue("loop-tests opt-in", Boolean.getBoolean("loop.tests"));
    File segDir = new File("../segments4");
    if (!segDir.exists()) segDir = new File("segments4");
    File segFile = new File(segDir, "E5_N45.rd5");
    Assume.assumeTrue("segment present at " + segFile.getAbsolutePath(), segFile.exists());
    File pf = profileFile("fastbike");
    Assume.assumeTrue("profile present", pf != null);

    List<OsmNodeNamed> wplist = new ArrayList<>();
    { OsmNodeNamed n = new OsmNodeNamed(); n.name = "from"; n.ilon = 187591524; n.ilat = 137562900; wplist.add(n); }
    { OsmNodeNamed n = new OsmNodeNamed(); n.name = "via1"; n.ilon = 187491117; n.ilat = 137490902; wplist.add(n); }
    { OsmNodeNamed n = new OsmNodeNamed(); n.name = "via2"; n.ilon = 187477265; n.ilat = 137453950; wplist.add(n); }
    { OsmNodeNamed n = new OsmNodeNamed(); n.name = "via3"; n.ilon = 187569451; n.ilat = 137420944; wplist.add(n); }
    { OsmNodeNamed n = new OsmNodeNamed(); n.name = "via4"; n.ilon = 187648493; n.ilat = 137423820; wplist.add(n); }
    { OsmNodeNamed n = new OsmNodeNamed(); n.name = "to"; n.ilon = 187698504; n.ilat = 137514578; wplist.add(n); }

    RoutingContext rctx = new RoutingContext();
    rctx.localFunction = pf.getAbsolutePath();
    RoutingEngine re = new RoutingEngine(null, null, segDir, wplist, rctx);
    re.quite = true;
    re.doRun(0);

    String err = re.getErrorMessage();
    OsmTrack track = re.getFoundTrack();
    System.out.println("=== replay_dayout_1365663830_10_GRVL_106_mountainous_SUI_Bretzwil ===");
    System.out.println("  error: " + (err == null ? "PASSED" : err));
    if (track != null && track.nodes != null) {
      System.out.println("  route: " + track.distance + "m / " + track.nodes.size() + " nodes");
      HostileStretch hs = RoundTripQualityGate.worstHostileStretchPaved(track);
      if (hs.isPresent()) {
        System.out.printf("  worst-hostile: %dm at (%.6f, %.6f) tags: %s%n",
          hs.meters, hs.startLat(), hs.startLon(), hs.startTags);
      } else {
        System.out.println("  hostile: none");
      }
      Assert.assertNull("expected clean point-to-point route through Komoot waypoints: " + err, err);
      Assert.assertTrue("expected no hostile stretch over the gate cap",
        !hs.isPresent() || hs.meters <= RoundTripQualityGate.MAX_CONTIGUOUS_HOSTILE_METERS);
    } else {
      Assert.fail("brouter returned no track: " + err);
    }
  }

  /**
   * Replay of "dayout__1405919111__#11 ALRD 100 hilly GER Heitersheim" — feeds the
   * sampled waypoints to brouter and reports whether the resulting
   * point-to-point route is clean enough to satisfy the round-trip
   * quality gate. Failure means the routing engine (with the
   * fastbike profile) can't reproduce the known-good route
   * even when given the right waypoints.
   */
  @Test
  public void replay_dayout_1405919111_11_ALRD_100_hilly_GER_Heitersheim() throws Exception {
    Assume.assumeTrue("loop-tests opt-in", Boolean.getBoolean("loop.tests"));
    File segDir = new File("../segments4");
    if (!segDir.exists()) segDir = new File("segments4");
    File segFile = new File(segDir, "E5_N45.rd5");
    Assume.assumeTrue("segment present at " + segFile.getAbsolutePath(), segFile.exists());
    File pf = profileFile("fastbike");
    Assume.assumeTrue("profile present", pf != null);

    List<OsmNodeNamed> wplist = new ArrayList<>();
    { OsmNodeNamed n = new OsmNodeNamed(); n.name = "from"; n.ilon = 187591524; n.ilat = 137562900; wplist.add(n); }
    { OsmNodeNamed n = new OsmNodeNamed(); n.name = "via1"; n.ilon = 187600764; n.ilat = 137667328; wplist.add(n); }
    { OsmNodeNamed n = new OsmNodeNamed(); n.name = "via2"; n.ilon = 187554887; n.ilat = 137808210; wplist.add(n); }
    { OsmNodeNamed n = new OsmNodeNamed(); n.name = "via3"; n.ilon = 187663518; n.ilat = 137849278; wplist.add(n); }
    { OsmNodeNamed n = new OsmNodeNamed(); n.name = "via4"; n.ilon = 187601783; n.ilat = 137784164; wplist.add(n); }
    { OsmNodeNamed n = new OsmNodeNamed(); n.name = "to"; n.ilon = 187623819; n.ilat = 137654207; wplist.add(n); }

    RoutingContext rctx = new RoutingContext();
    rctx.localFunction = pf.getAbsolutePath();
    RoutingEngine re = new RoutingEngine(null, null, segDir, wplist, rctx);
    re.quite = true;
    re.doRun(0);

    String err = re.getErrorMessage();
    OsmTrack track = re.getFoundTrack();
    System.out.println("=== replay_dayout_1405919111_11_ALRD_100_hilly_GER_Heitersheim ===");
    System.out.println("  error: " + (err == null ? "PASSED" : err));
    if (track != null && track.nodes != null) {
      System.out.println("  route: " + track.distance + "m / " + track.nodes.size() + " nodes");
      HostileStretch hs = RoundTripQualityGate.worstHostileStretchPaved(track);
      if (hs.isPresent()) {
        System.out.printf("  worst-hostile: %dm at (%.6f, %.6f) tags: %s%n",
          hs.meters, hs.startLat(), hs.startLon(), hs.startTags);
      } else {
        System.out.println("  hostile: none");
      }
      Assert.assertNull("expected clean point-to-point route through Komoot waypoints: " + err, err);
      Assert.assertTrue("expected no hostile stretch over the gate cap",
        !hs.isPresent() || hs.meters <= RoundTripQualityGate.MAX_CONTIGUOUS_HOSTILE_METERS);
    } else {
      Assert.fail("brouter returned no track: " + err);
    }
  }

  /**
   * Replay of "dayout__1470347440__#19 SEARCHING FOR PAVE longer" — feeds the
   * sampled waypoints to brouter and reports whether the resulting
   * point-to-point route is clean enough to satisfy the round-trip
   * quality gate. Failure means the routing engine (with the
   * fastbike profile) can't reproduce the known-good route
   * even when given the right waypoints.
   */
  @Test
  public void replay_dayout_1470347440_19_SEARCHING_FOR_PAVE_longer() throws Exception {
    Assume.assumeTrue("loop-tests opt-in", Boolean.getBoolean("loop.tests"));
    File segDir = new File("../segments4");
    if (!segDir.exists()) segDir = new File("segments4");
    File segFile = new File(segDir, "E5_N45.rd5");
    Assume.assumeTrue("segment present at " + segFile.getAbsolutePath(), segFile.exists());
    File pf = profileFile("fastbike");
    Assume.assumeTrue("profile present", pf != null);

    List<OsmNodeNamed> wplist = new ArrayList<>();
    { OsmNodeNamed n = new OsmNodeNamed(); n.name = "from"; n.ilon = 187591494; n.ilat = 137562891; wplist.add(n); }
    { OsmNodeNamed n = new OsmNodeNamed(); n.name = "via1"; n.ilon = 187510853; n.ilat = 137661729; wplist.add(n); }
    { OsmNodeNamed n = new OsmNodeNamed(); n.name = "via2"; n.ilon = 187374614; n.ilat = 137656108; wplist.add(n); }
    { OsmNodeNamed n = new OsmNodeNamed(); n.name = "via3"; n.ilon = 187289159; n.ilat = 137590458; wplist.add(n); }
    { OsmNodeNamed n = new OsmNodeNamed(); n.name = "via4"; n.ilon = 187389404; n.ilat = 137531673; wplist.add(n); }
    { OsmNodeNamed n = new OsmNodeNamed(); n.name = "to"; n.ilon = 187486518; n.ilat = 137511021; wplist.add(n); }

    RoutingContext rctx = new RoutingContext();
    rctx.localFunction = pf.getAbsolutePath();
    RoutingEngine re = new RoutingEngine(null, null, segDir, wplist, rctx);
    re.quite = true;
    re.doRun(0);

    String err = re.getErrorMessage();
    OsmTrack track = re.getFoundTrack();
    System.out.println("=== replay_dayout_1470347440_19_SEARCHING_FOR_PAVE_longer ===");
    System.out.println("  error: " + (err == null ? "PASSED" : err));
    if (track != null && track.nodes != null) {
      System.out.println("  route: " + track.distance + "m / " + track.nodes.size() + " nodes");
      HostileStretch hs = RoundTripQualityGate.worstHostileStretchPaved(track);
      if (hs.isPresent()) {
        System.out.printf("  worst-hostile: %dm at (%.6f, %.6f) tags: %s%n",
          hs.meters, hs.startLat(), hs.startLon(), hs.startTags);
      } else {
        System.out.println("  hostile: none");
      }
      Assert.assertNull("expected clean point-to-point route through Komoot waypoints: " + err, err);
      Assert.assertTrue("expected no hostile stretch over the gate cap",
        !hs.isPresent() || hs.meters <= RoundTripQualityGate.MAX_CONTIGUOUS_HOSTILE_METERS);
    } else {
      Assert.fail("brouter returned no track: " + err);
    }
  }

  /**
   * Replay of "dayout__1477562083__#15 SEARCHING FOR PAVE long" — feeds the
   * sampled waypoints to brouter and reports whether the resulting
   * point-to-point route is clean enough to satisfy the round-trip
   * quality gate. Failure means the routing engine (with the
   * fastbike profile) can't reproduce the known-good route
   * even when given the right waypoints.
   */
  @Test
  public void replay_dayout_1477562083_15_SEARCHING_FOR_PAVE_long() throws Exception {
    Assume.assumeTrue("loop-tests opt-in", Boolean.getBoolean("loop.tests"));
    File segDir = new File("../segments4");
    if (!segDir.exists()) segDir = new File("segments4");
    File segFile = new File(segDir, "E5_N45.rd5");
    Assume.assumeTrue("segment present at " + segFile.getAbsolutePath(), segFile.exists());
    File pf = profileFile("fastbike");
    Assume.assumeTrue("profile present", pf != null);

    List<OsmNodeNamed> wplist = new ArrayList<>();
    { OsmNodeNamed n = new OsmNodeNamed(); n.name = "from"; n.ilon = 187591494; n.ilat = 137562891; wplist.add(n); }
    { OsmNodeNamed n = new OsmNodeNamed(); n.name = "via1"; n.ilon = 187521808; n.ilat = 137657917; wplist.add(n); }
    { OsmNodeNamed n = new OsmNodeNamed(); n.name = "via2"; n.ilon = 187370002; n.ilat = 137681335; wplist.add(n); }
    { OsmNodeNamed n = new OsmNodeNamed(); n.name = "via3"; n.ilon = 187332756; n.ilat = 137602604; wplist.add(n); }
    { OsmNodeNamed n = new OsmNodeNamed(); n.name = "via4"; n.ilon = 187399769; n.ilat = 137492685; wplist.add(n); }
    { OsmNodeNamed n = new OsmNodeNamed(); n.name = "to"; n.ilon = 187500540; n.ilat = 137535782; wplist.add(n); }

    RoutingContext rctx = new RoutingContext();
    rctx.localFunction = pf.getAbsolutePath();
    RoutingEngine re = new RoutingEngine(null, null, segDir, wplist, rctx);
    re.quite = true;
    re.doRun(0);

    String err = re.getErrorMessage();
    OsmTrack track = re.getFoundTrack();
    System.out.println("=== replay_dayout
wplist.add(n); }

    RoutingContext rctx = new RoutingContext();
    rctx.localFunction = pf.getAbsolutePath();
    RoutingEngine re = new RoutingEngine(null, null, segDir, wplist, rctx);
    re.quite = true;
    re.doRun(0);

    String err = re.getErrorMessage();
    OsmTrack track = re.getFoundTrack();
    System.out.println("=== replay_dayout_539365209_07_ROAD_80_hilly_SUI_Arboldswil ===");
    System.out.println("  error: " + (err == null ? "PASSED" : err));
    if (track != null && track.nodes != null) {
      System.out.println("  route: " + track.distance + "m / " + track.nodes.size() + " nodes");
      HostileStretch hs = RoundTripQualityGate.worstHostileStretchPaved(track);
      if (hs.isPresent()) {
        System.out.printf("  worst-hostile: %dm at (%.6f, %.6f) tags: %s%n",
          hs.meters, hs.startLat(), hs.startLon(), hs.startTags);
      } else {
        System.out.println("  hostile: none");
      }
      Assert.assertNull("expected clean point-to-point route through Komoot waypoints: " + err, err);
      Assert.assertTrue("expected no hostile stretch over the gate cap",
        !hs.isPresent() || hs.meters <= RoundTripQualityGate.MAX_CONTIGUOUS_HOSTILE_METERS);
    } else {
      Assert.fail("brouter returned no track: " + err);
    }
  }

  /**
   * Replay of "dayout__540844650__#08 ALRD 98 hilly GER Hochblauen" — feeds the
   * sampled waypoints to brouter and reports whether the resulting
   * point-to-point route is clean enough to satisfy the round-trip
   * quality gate. Failure means the routing engine (with the
   * fastbike profile) can't reproduce the known-good route
   * even when given the right waypoints.
   */
  @Test
  public void replay_dayout_540844650_08_ALRD_98_hilly_GER_Hochblauen() throws Exception {
    Assume.assumeTrue("loop-tests opt-in", Boolean.getBoolean("loop.tests"));
    File segDir = new File("../segments4");
    if (!segDir.exists()) segDir = new File("segments4");
    File segFile = new File(segDir, "E5_N45.rd5");
    Assume.assumeTrue("segment present at " + segFile.getAbsolutePath(), segFile.exists());
    File pf = profileFile("fastbike");
    Assume.assumeTrue("profile present", pf != null);

    List<OsmNodeNamed> wplist = new ArrayList<>();
    { OsmNodeNamed n = new OsmNodeNamed(); n.name = "from"; n.ilon = 187591524; n.ilat = 137562900; wplist.add(n); }
    { OsmNodeNamed n = new OsmNodeNamed(); n.name = "via1"; n.ilon = 187623492; n.ilat = 137632996; wplist.add(n); }
    { OsmNodeNamed n = new OsmNodeNamed(); n.name = "via2"; n.ilon = 187712943; n.ilat = 137745917; wplist.add(n); }
    { OsmNodeNamed n = new OsmNodeNamed(); n.name = "via3"; n.ilon = 187699007; n.ilat = 137785773; wplist.add(n); }
    { OsmNodeNamed n = new OsmNodeNamed(); n.name = "via4"; n.ilon = 187622216; n.ilat = 137809165; wplist.add(n); }
    { OsmNodeNamed n = new OsmNodeNamed(); n.name = "to"; n.ilon = 187518656; n.ilat = 137688638; wplist.add(n); }

    RoutingContext rctx = new RoutingContext();
    rctx.localFunction = pf.getAbsolutePath();
    RoutingEngine re = new RoutingEngine(null, null, segDir, wplist, rctx);
    re.quite = true;
    re.doRun(0);

    String err = re.getErrorMessage();
    OsmTrack track = re.getFoundTrack();
    System.out.println("=== replay_dayout_540844650_08_ALRD_98_hilly_GER_Hochblauen ===");
    System.out.println("  error: " + (err == null ? "PASSED" : err));
    if (track != null && track.nodes != null) {
      System.out.println("  route: " + track.distance + "m / " + track.nodes.size() + " nodes");
      HostileStretch hs = RoundTripQualityGate.worstHostileStretchPaved(track);
      if (hs.isPresent()) {
        System.out.printf("  worst-hostile: %dm at (%.6f, %.6f) tags: %s%n",
          hs.meters, hs.startLat(), hs.startLon(), hs.startTags);
      } else {
        System.out.println("  hostile: none");
      }
      Assert.assertNull("expected clean point-to-point route through Komoot waypoints: " + err, err);
      Assert.assertTrue("expected no hostile stretch over the gate cap",
        !hs.isPresent() || hs.meters <= RoundTripQualityGate.MAX_CONTIGUOUS_HOSTILE_METERS);
    } else {
      Assert.fail("brouter returned no track: " + err);
    }
  }

  /**
   * Replay of "dayout__683137545__#17 WHITE NOISE 2022" — feeds the
   * sampled waypoints to brouter and reports whether the resulting
   * point-to-point route is clean enough to satisfy the round-trip
   * quality gate. Failure means the routing engine (with the
   * fastbike profile) can't reproduce the known-good route
   * even when given the right waypoints.
   */
  @Test
  public void replay_dayout_683137545_17_WHITE_NOISE_2022() throws Exception {
    Assume.assumeTrue("loop-tests opt-in", Boolean.getBoolean("loop.tests"));
    File segDir = new File("../segments4");
    if (!segDir.exists()) segDir = new File("segments4");
    File segFile = new File(segDir, "E5_N45.rd5");
    Assume.assumeTrue("segment present at " + segFile.getAbsolutePath(), segFile.exists());
    File pf = profileFile("fastbike");
    Assume.assumeTrue("profile present", pf != null);

    List<OsmNodeNamed> wplist = new ArrayList<>();
    { OsmNodeNamed n = new OsmNodeNamed(); n.name = "from"; n.ilon = 187591524; n.ilat = 137562900; wplist.add(n); }
    { OsmNodeNamed n = new OsmNodeNamed(); n.name = "via1"; n.ilon = 187468545; n.ilat = 137535024; wplist.add(n); }
    { OsmNodeNamed n = new OsmNodeNamed(); n.name = "via2"; n.ilon = 187300091; n.ilat = 137503298; wplist.add(n); }
    { OsmNodeNamed n = new OsmNodeNamed(); n.name = "via3"; n.ilon = 187267408; n.ilat = 137538179; wplist.add(n); }
    { OsmNodeNamed n = new OsmNodeNamed(); n.name = "via4"; n.ilon = 187353985; n.ilat = 137640412; wplist.add(n); }
    { OsmNodeNamed n = new OsmNodeNamed(); n.name = "to"; n.ilon = 187469234; n.ilat = 137569854; wplist.add(n); }

    RoutingContext rctx = new RoutingContext();
    rctx.localFunction = pf.getAbsolutePath();
    RoutingEngine re = new RoutingEngine(null, null, segDir, wplist, rctx);
    re.quite = true;
    re.doRun(0);

    String err = re.getErrorMessage();
    OsmTrack track = re.getFoundTrack();
    System.out.println("=== replay_dayout_683137545_17_WHITE_NOISE_2022 ===");
    System.out.println("  error: " + (err == null ? "PASSED" : err));
    if (track != null && track.nodes != null) {
      System.out.println("  route: " + track.distance + "m / " + track.nodes.size() + " nodes");
      HostileStretch hs = RoundTripQualityGate.worstHostileStretchPaved(track);
      if (hs.isPresent()) {
        System.out.printf("  worst-hostile: %dm at (%.6f, %.6f) tags: %s%n",
          hs.meters, hs.startLat(), hs.startLon(), hs.startTags);
      } else {
        System.out.println("  hostile: none");
      }
      Assert.assertNull("expected clean point-to-point route through Komoot waypoints: " + err, err);
      Assert.assertTrue("expected no hostile stretch over the gate cap",
        !hs.isPresent() || hs.meters <= RoundTripQualityGate.MAX_CONTIGUOUS_HOSTILE_METERS);
    } else {
      Assert.fail("brouter returned no track: " + err);
    }
  }

  /**
   * Replay of "dayout__715956655__#13 DE RONDE 2022" — feeds the
   * sampled waypoints to brouter and reports whether the resulting
   * point-to-point route is clean enough to satisfy the round-trip
   * quality gate. Failure means the routing engine (with the
   * fastbike profile) can't reproduce the known-good route
   * even when given the right waypoints.
   */
  @Test
  public void replay_dayout_715956655_13_DE_RONDE_2022() throws Exception {
    Assume.assumeTrue("loop-tests opt-in", Boolean.getBoolean("loop.tests"));
    File segDir = new File("../segments4");
    if (!segDir.exists()) segDir = new File("segments4");
    File segFile = new File(segDir, "E5_N45.rd5");
    Assume.assumeTrue("segment present at " + segFile.getAbsolutePath(), segFile.exists());
    File pf = profileFile("fastbike");
    Assume.assumeTrue("profile present", pf != null);

    List<OsmNodeNamed> wplist = new ArrayList<>();
    { OsmNodeNamed n = new OsmNodeNamed(); n.name = "from"; n.ilon = 187591524; n.ilat = 137562900; wplist.add(n); }
    { OsmNodeNamed n = new OsmNodeNamed(); n.name = "via1"; n.ilon = 187648871; n.ilat = 137513466; wplist.add(n); }
    { OsmNodeNamed n = new OsmNodeNamed(); n.name = "via2"; n.ilon = 187694296; n.ilat = 137479556; wplist.add(n); }
    { OsmNodeNamed n = new OsmNodeNamed(); n.name = "via3"; n.ilon = 187644964; n.ilat = 137423663; wplist.add(n); }
    { OsmNodeNamed n = new OsmNodeNamed(); n.name = "via4"; n.ilon = 187642209; n.ilat = 137460535; wplist.add(n); }
    { OsmNodeNamed n = new OsmNodeNamed(); n.name = "to"; n.ilon = 187601922; n.ilat = 137497074; wplist.add(n); }

    RoutingContext rctx = new RoutingContext();
    rctx.localFunction = pf.getAbsolutePath();
    RoutingEngine re = new RoutingEngine(null, null, segDir, wplist, rctx);
    re.quite = true;
    re.doRun(0);

    String err = re.getErrorMessage();
    OsmTrack track = re.getFoundTrack();
    System.out.println("=== replay_dayout_715956655_13_DE_RONDE_2022 ===");
    System.out.println("  error: " + (err == null ? "PASSED" : err));
    if (track != null && track.nodes != null) {
      System.out.println("  route: " + track.distance + "m / " + track.nodes.size() + " nodes");
      HostileStretch hs = RoundTripQualityGate.worstHostileStretchPaved(track);
      if (hs.isPresent()) {
        System.out.printf("  worst-hostile: %dm at (%.6f, %.6f) tags: %s%n",
          hs.meters, hs.startLat(), hs.startLon(), hs.startTags);
      } else {
        System.out.println("  hostile: none");
      }
      Assert.assertNull("expected clean point-to-point route through Komoot waypoints: " + err, err);
      Assert.assertTrue("expected no hostile stretch over the gate cap",
        !hs.isPresent() || hs.meters <= RoundTripQualityGate.MAX_CONTIGUOUS_HOSTILE_METERS);
    } else {
      Assert.fail("brouter returned no track: " + err);
    }
  }

  /**
   * Replay of "dayout__901940052__#20 GIRLS NIGHT" — feeds the
   * sampled waypoints to brouter and reports whether the resulting
   * point-to-point route is clean enough to satisfy the round-trip
   * quality gate. Failure means the routing engine (with the
   * fastbike profile) can't reproduce the known-good route
   * even when given the right waypoints.
   */
  @Test
  public void replay_dayout_901940052_20_GIRLS_NIGHT() throws Exception {
    Assume.assumeTrue("loop-tests opt-in", Boolean.getBoolean("loop.tests"));
    File segDir = new File("../segments4");
    if (!segDir.exists()) segDir = new File("segments4");
    File segFile = new File(segDir, "E5_N45.rd5");
    Assume.assumeTrue("segment present at " + segFile.getAbsolutePath(), segFile.exists());
    File pf = profileFile("fastbike");
    Assume.assumeTrue("profile present", pf != null);

    List<OsmNodeNamed> wplist = new ArrayList<>();
    { OsmNodeNamed n = new OsmNodeNamed(); n.name = "from"; n.ilon = 187591524; n.ilat = 137562900; wplist.add(n); }
    { OsmNodeNamed n = new OsmNodeNamed(); n.name = "via1"; n.ilon = 187452032; n.ilat = 137522965; wplist.add(n); }
    { OsmNodeNamed n = new OsmNodeNamed(); n.name = "via2"; n.ilon = 187255679; n.ilat = 137502766; wplist.add(n); }
    { OsmNodeNamed n = new OsmNodeNamed(); n.name = "via3"; n.ilon = 187011394; n.ilat = 137524164; wplist.add(n); }
    { OsmNodeNamed n = new OsmNodeNamed(); n.name = "via4"; n.ilon = 187207924; n.ilat = 137618493; wplist.add(n); }
    { OsmNodeNamed n = new OsmNodeNamed(); n.name = "to"; n.ilon = 187447338; n.ilat = 137566509; wplist.add(n); }

    RoutingContext rctx = new RoutingContext();
    rctx.localFunction = pf.getAbsolutePath();
    RoutingEngine re = new RoutingEngine(null, null, segDir, wplist, rctx);
    re.quite = true;
    re.doRun(0);

    String err = re.getErrorMessage();
    OsmTrack track = re.getFoundTrack();
    System.out.println("=== replay_dayout_901940052_20_GIRLS_NIGHT ===");
    System.out.println("  error: " + (err == null ? "PASSED" : err));
    if (track != null && track.nodes != null) {
      System.out.println("  route: " + track.distance + "m / " + track.nodes.size() + " nodes");
      HostileStretch hs = RoundTripQualityGate.worstHostileStretchPaved(track);
      if (hs.isPresent()) {
        System.out.printf("  worst-hostile: %dm at (%.6f, %.6f) tags: %s%n",
          hs.meters, hs.startLat(), hs.startLon(), hs.startTags);
      } else {
        System.out.println("  hostile: none");
      }
      Assert.assertNull("expected clean point-to-point route through Komoot waypoints: " + err, err);
      Assert.assertTrue("expected no hostile stretch over the gate cap",
        !hs.isPresent() || hs.meters <= RoundTripQualityGate.MAX_CONTIGUOUS_HOSTILE_METERS);
    } else {
      Assert.fail("brouter returned no track: " + err);
    }
  }

  /**
   * Replay of "dayout__934106085__#09 ROTFL 2022" — feeds the
   * sampled waypoints to brouter and reports whether the resulting
   * point-to-point route is clean enough to satisfy the round-trip
   * quality gate. Failure means the routing engine (with the
   * fastbike profile) can't reproduce the known-good route
   * even when given the right waypoints.
   */
  @Test
  public void replay_dayout_934106085_09_ROTFL_2022() throws Exception {
    Assume.assumeTrue("loop-tests opt-in", Boolean.getBoolean("loop.tests"));
    File segDir = new File("../segments4");
    if (!segDir.exists()) segDir = new File("segments4");
    File segFile = new File(segDir, "E5_N45.rd5");
    Assume.assumeTrue("segment present at " + segFile.getAbsolutePath(), segFile.exists());
    File pf = profileFile("fastbike");
    Assume.assumeTrue("profile present", pf != null);

    List<OsmNodeNamed> wplist = new ArrayList<>();
    { OsmNodeNamed n = new OsmNodeNamed(); n.name = "from"; n.ilon = 187591524; n.ilat = 137562900; wplist.add(n); }
    { OsmNodeNamed n = new OsmNodeNamed(); n.name = "via1"; n.ilon = 187761190; n.ilat = 137614820; wplist.add(n); }
    { OsmNodeNamed n = new OsmNodeNamed(); n.name = "via2"; n.ilon = 187896840; n.ilat = 137655962; wplist.add(n); }
    { OsmNodeNamed n = new OsmNodeNamed(); n.name = "via3"; n.ilon = 187800091; n.ilat = 137716775; wplist.add(n); }
    { OsmNodeNamed n = new OsmNodeNamed(); n.name = "via4"; n.ilon = 187714429; n.ilat = 137739069; wplist.add(n); }
    { OsmNodeNamed n = new OsmNodeNamed(); n.name = "to"; n.ilon = 187550650; n.ilat = 137695124; wplist.add(n); }

    RoutingContext rctx = new RoutingContext();
    rctx.localFunction = pf.getAbsolutePath();
    RoutingEngine re = new RoutingEngine(null, null, segDir, wplist, rctx);
    re.quite = true;
    re.doRun(0);

    String err = re.getErrorMessage();
    OsmTrack track = re.getFoundTrack();
    System.out.println("=== replay_dayout_934106085_09_ROTFL_2022 ===");
    System.out.println("  error: " + (err == null ? "PASSED" : err));
    if (track != null && track.nodes != null) {
      System.out.println("  route: " + track.distance + "m / " + track.nodes.size() + " nodes");
      HostileStretch hs = RoundTripQualityGate.worstHostileStretchPaved(track);
      if (hs.isPresent()) {
        System.out.printf("  worst-hostile: %dm at (%.6f, %.6f) tags: %s%n",
          hs.meters, hs.startLat(), hs.startLon(), hs.startTags);
      } else {
        System.out.println("  hostile: none");
      }
      Assert.assertNull("expected clean point-to-point route through Komoot waypoints: " + err, err);
      Assert.assertTrue("expected no hostile stretch over the gate cap",
        !hs.isPresent() || hs.meters <= RoundTripQualityGate.MAX_CONTIGUOUS_HOSTILE_METERS);
    } else {
      Assert.fail("brouter returned no track: " + err);
    }
  }

  /**
   * Replay of "lunch__278259778__#02 ROAD 50 hilly SUI Seewen (1)" — feeds the
   * sampled waypoints to brouter and reports whether the resulting
   * point-to-point route is clean enough to satisfy the round-trip
   * quality gate. Failure means the routing engine (with the
   * fastbike profile) can't reproduce the known-good route
   * even when given the right waypoints.
   */
  @Test
  public void replay_lunch_278259778_02_ROAD_50_hilly_SUI_Seewen_1_() throws Exception {
    Assume.assumeTrue("loop-tests opt-in", Boolean.getBoolean("loop.tests"));
    File segDir = new File("../segments4");
    if (!segDir.exists()) segDir = new File("segments4");
    File segFile = new File(segDir, "E5_N45.rd5");
    Assume.assumeTrue("segment present at " + segFile.getAbsolutePath(), segFile.exists());
    File pf = profileFile("fastbike");
    Assume.assumeTrue("profile present", pf != null);

    List<OsmNodeNamed> wplist = new ArrayList<>();
    { OsmNodeNamed n = new OsmNodeNamed(); n.name = "from"; n.ilon = 187591524; n.ilat = 137562900; wplist.add(n); }
    { OsmNodeNamed n = new OsmNodeNamed(); n.name = "via1"; n.ilon = 187637799; n.ilat = 137523334; wplist.add(n); }
    { OsmNodeNamed n = new OsmNodeNamed(); n.name = "via2"; n.ilon = 187655195; n.ilat = 137493088; wplist.add(n); }
    { OsmNodeNamed n = new OsmNodeNamed(); n.name = "via3"; n.ilon = 187642815; n.ilat = 137449806; wplist.add(n); }
    { OsmNodeNamed n = new OsmNodeNamed(); n.name = "via4"; n.ilon = 187601966; n.ilat = 137444672; wplist.add(n); }
    { OsmNodeNamed n = new OsmNodeNamed(); n.name = "to"; n.ilon = 187620825; n.ilat = 137510340; wplist.add(n); }

    RoutingContext rctx = new RoutingContext();
    rctx.localFunction = pf.getAbsolutePath();
    RoutingEngine re = new RoutingEngine(null, null, segDir, wplist, rctx);
    re.quite = true;
    re.doRun(0);

    String err = re.getErrorMessage();
    OsmTrack track = re.getFoundTrack();
    System.out.println("=== replay_lunch_278259778_02_ROAD_50_hilly_SUI_Seewen_1_ ===");
    System.out.println("  error: " + (err == null ? "PASSED" : err));
    if (track != null && track.nodes != null) {
      System.out.println("  route: " + track.distance + "m / " + track.nodes.size() + " nodes");
      HostileStretch hs = RoundTripQualityGate.worstHostileStretchPaved(track);
      if (hs.isPresent()) {
        System.out.printf("  worst-hostile: %dm at (%.6f, %.6f) tags: %s%n",
          hs.meters, hs.startLat(), hs.startLon(), hs.startTags);
      } else {
        System.out.println("  hostile: none");
      }
      Assert.assertNull("expected clean point-to-point route through Komoot waypoints: " + err, err);
      Assert.assertTrue("expected no hostile stretch over the gate cap",
        !hs.isPresent() || hs.meters <= RoundTripQualityGate.MAX_CONTIGUOUS_HOSTILE_METERS);
    } else {
      Assert.fail("brouter returned no track: " + err);
    }
  }

  /**
   * Replay of "lunch__278259778__#02 ROAD 50 hilly SUI Seewen" — feeds the
   * sampled waypoints to brouter and reports whether the resulting
   * point-to-point route is clean enough to satisfy the round-trip
   * quality gate. Failure means the routing engine (with the
   * fastbike profile) can't reproduce the known-good route
   * even when given the right waypoints.
   */
  @Test
  public void replay_lunch_278259778_02_ROAD_50_hilly_SUI_Seewen() throws Exception {
    Assume.assumeTrue("loop-tests opt-in", Boolean.getBoolean("loop.tests"));
    File segDir = new File("../segments4");
    if (!segDir.exists()) segDir = new File("segments4");
    File segFile = new File(segDir, "E5_N45.rd5");
    Assume.assumeTrue("segment present at " + segFile.getAbsolutePath(), segFile.exists());
    File pf = profileFile("fastbike");
    Assume.assumeTrue("profile present", pf != null);

    List<OsmNodeNamed> wplist = new ArrayList<>();
    { OsmNodeNamed n = new OsmNodeNamed(); n.name = "from"; n.ilon = 187591524; n.ilat = 137562900; wplist.add(n); }
    { OsmNodeNamed n = new OsmNodeNamed(); n.name = "via1"; n.ilon = 187637799; n.ilat = 137523334; wplist.add(n); }
    { OsmNodeNamed n = new OsmNodeNamed(); n.name = "via2"; n.ilon = 187655195; n.ilat = 137493088; wplist.add(n); }
    { OsmNodeNamed n = new OsmNodeNamed(); n.name = "via3"; n.ilon = 187642815; n.ilat = 137449806; wplist.add(n); }
    { OsmNodeNamed n = new OsmNodeNamed(); n.name = "via4"; n.ilon = 187601966; n.ilat = 137444672; wplist.add(n); }
    { OsmNodeNamed n = new OsmNodeNamed(); n.name = "to"; n.ilon = 187620825; n.ilat = 137510340; wplist.add(n); }

    RoutingContext rctx = new RoutingContext();
    rctx.localFunction = pf.getAbsolutePath();
    RoutingEngine re = new RoutingEngine(null, null, segDir, wplist, rctx);
    re.quite = true;
    re.doRun(0);

    String err = re.getErrorMessage();
    OsmTrack track = re.getFoundTrack();
    System.out.println("=== replay_lunch_278259778_02_ROAD_50_hilly_SUI_Seewen ===");
    System.out.println("  error: " + (err == null ? "PASSED" : err));
    if (track != null && track.nodes != null) {
      System.out.println("  route: " + track.distance + "m / " + track.nodes.size() + " nodes");
      HostileStretch hs = RoundTripQualityGate.worstHostileStretchPaved(track);
      if (hs.isPresent()) {
        System.out.printf("  worst-hostile: %dm at (%.6f, %.6f) tags: %s%n",
          hs.meters, hs.startLat(), hs.startLon(), hs.startTags);
      } else {
        System.out.println("  hostile: none");
      }
      Assert.assertNull("expected clean point-to-point route through Komoot waypoints: " + err, err);
      Assert.assertTrue("expected no hostile stretch over the gate cap",
        !hs.isPresent() || hs.meters <= RoundTripQualityGate.MAX_CONTIGUOUS_HOSTILE_METERS);
    } else {
      Assert.fail("brouter returned no track: " + err);
    }
  }

  /**
   * Replay of "lunch__286951681__#01 ROAD 50 hilly GER Karsau (1)" — feeds the
   * sampled waypoints to brouter and reports whether the resulting
   * point-to-point route is clean enough to satisfy the round-trip
   * quality gate. Failure means the routing engine (with the
   * fastbike profile) can't reproduce the known-good route
   * even when given the right waypoints.
   */
  @Test
  public void replay_lunch_286951681_01_ROAD_50_hilly_GER_Karsau_1_() throws Exception {
    Assume.assumeTrue("loop-tests opt-in", Boolean.getBoolean("loop.tests"));
    File segDir = new File("../segments4");
    if (!segDir.exists()) segDir = new File("segments4");
    File segFile = new File(segDir, "E5_N45.rd5");
    Assume.assumeTrue("segment present at " + segFile.getAbsolutePath(), segFile.exists());
    File pf = profileFile("fastbike");
    Assume.assumeTrue("profile present", pf != null);

    List<OsmNodeNamed> wplist = new ArrayList<>();
    { OsmNodeNamed n = new OsmNodeNamed(); n.name = "from"; n.ilon = 187591524; n.ilat = 137562900; wplist.add(n); }
    { OsmNodeNamed n = new OsmNodeNamed(); n.name = "via1"; n.ilon = 187615778; n.ilat = 137575319; wplist.add(n); }
    { OsmNodeNamed n = new OsmNodeNamed(); n.name = "via2"; n.ilon = 187695016; n.ilat = 137639024; wplist.add(n); }
    { OsmNodeNamed n = new OsmNodeNamed(); n.name = "via3"; n.ilon = 187756924; n.ilat = 137600041; wplist.add(n); }
    { OsmNodeNamed n = new OsmNodeNamed(); n.name = "via4"; n.ilon = 187772554; n.ilat = 137564259; wplist.add(n); }
    { OsmNodeNamed n = new OsmNodeNamed(); n.name = "to"; n.ilon = 187667139; n.ilat = 137549673; wplist.add(n); }

    RoutingContext rctx = new RoutingContext();
    rctx.localFunction = pf.getAbsolutePath();
    RoutingEngine re = new RoutingEngine(null, null, segDir, wplist, rctx);
    re.quite = true;
    re.doRun(0);

    String err = re.getErrorMessage();
    OsmTrack track = re.getFoundTrack();
    System.out.println("=== replay_lunch_286951681_01_ROAD_50_hilly_GER_Karsau_1_ ===");
    System.out.println("  error: " + (err == null ? "PASSED" : err));
    if (track != null && track.nodes != null) {
      System.out.println("  route: " + track.distance + "m / " + track.nodes.size() + " nodes");
      HostileStretch hs = RoundTripQualityGate.worstHostileStretchPaved(track);
      if (hs.isPresent()) {
        System.out.printf("  worst-hostile: %dm at (%.6f, %.6f) tags: %s%n",
          hs.meters, hs.startLat(), hs.startLon(), hs.startTags);
      } else {
        System.out.println("  hostile: none");
      }
      Assert.assertNull("expected clean point-to-point route through Komoot waypoints: " + err, err);
      Assert.assertTrue("expected no hostile stretch over the gate cap",
        !hs.isPresent() || hs.meters <= RoundTripQualityGate.MAX_CONTIGUOUS_HOSTILE_METERS);
    } else {
      Assert.fail("brouter returned no track: " + err);
    }
  }

  /**
   * Replay of "lunch__286951681__#01 ROAD 50 hilly GER Karsau" — feeds the
   * sampled waypoints to brouter and reports whether the resulting
   * point-to-point route is clean enough to satisfy the round-trip
   * quality gate. Failure means the routing engine (with the
   * fastbike profile) can't reproduce the known-good route
   * even when given the right waypoints.
   */
  @Test
  public void replay_lunch_286951681_01_ROAD_50_hilly_GER_Karsau() throws Exception {
    Assume.assumeTrue("loop-tests opt-in", Boolean.getBoolean("loop.tests"));
    File segDir = new File("../segments4");
    if (!segDir.exists()) segDir = new File("segments4");
    File segFile = new File(segDir, "E5_N45.rd5");
    Assume.assumeTrue("segment present at " + segFile.getAbsolutePath(), segFile.exists());
    File pf = profileFile("fastbike");
    Assume.assumeTrue("profile present", pf != null);

    List<OsmNodeNamed> wplist = new ArrayList<>();
    { OsmNodeNamed n = new OsmNodeNamed(); n.name = "from"; n.ilon = 187591524; n.ilat = 137562900; wplist.add(n); }
    { OsmNodeNamed n = new OsmNodeNamed(); n.name = "via1"; n.ilon = 187615778; n.ilat = 137575319; wplist.add(n); }
    { OsmNodeNamed n = new OsmNodeNamed(); n.name = "via2"; n.ilon = 187695016; n.ilat = 137639024; wplist.add(n); }
    { OsmNodeNamed n = new OsmNodeNamed(); n.name = "via3"; n.ilon = 187756924; n.ilat = 137600041; wplist.add(n); }
    { OsmNodeNamed n = new OsmNodeNamed(); n.name = "via4"; n.ilon = 187772554; n.ilat = 137564259; wplist.add(n); }
    { OsmNodeNamed n = new OsmNodeNamed(); n.name = "to"; n.ilon = 187667139; n.ilat = 137549673; wplist.add(n); }

    RoutingContext rctx = new RoutingContext();
    rctx.localFunction = pf.getAbsolutePath();
    RoutingEngine re = new RoutingEngine(null, null, segDir, wplist, rctx);
    re.quite = true;
    re.doRun(0);

    String err = re.getErrorMessage();
    OsmTrack track = re.getFoundTrack();
    System.out.println("=== replay_lunch_286951681_01_ROAD_50_hilly_GER_Karsau ===");
    System.out.println("  error: " + (err == null ? "PASSED" : err));
    if (track != null && track.nodes != null) {
      System.out.println("  route: " + track.distance + "m / " + track.nodes.size() + " nodes");
      HostileStretch hs = RoundTripQualityGate.worstHostileStretchPaved(track);
      if (hs.isPresent()) {
        System.out.printf("  worst-hostile: %dm at (%.6f, %.6f) tags: %s%n",
          hs.meters, hs.startLat(), hs.startLon(), hs.startTags);
      } else {
        System.out.println("  hostile: none");
      }
      Assert.assertNull("expected clean point-to-point route through Komoot waypoints: " + err, err);
      Assert.assertTrue("expected no hostile stretch over the gate cap",
        !hs.isPresent() || hs.meters <= RoundTripQualityGate.MAX_CONTIGUOUS_HOSTILE_METERS);
    } else {
      Assert.fail("brouter returned no track: " + err);
    }
  }

  /**
   * Replay of "lunch__302709329__#03 ROAD 60 hilly GER Scheideck (1)" — feeds the
   * sampled waypoints to brouter and reports whether the resulting
   * point-to-point route is clean enough to satisfy the round-trip
   * quality gate. Failure means the routing engine (with the
   * fastbike profile) can't reproduce the known-good route
   * even when given the right waypoints.
   */
  @Test
  public void replay_lunch_302709329_03_ROAD_60_hilly_GER_Scheideck_1_() throws Exception {
    Assume.assumeTrue("loop-tests opt-in", Boolean.getBoolean("loop.tests"));
    File segDir = new File("../segments4");
    if (!segDir.exists()) segDir = new File("segments4");
    File segFile = new File(segDir, "E5_N45.rd5");
    Assume.assumeTrue("segment present at " + segFile.getAbsolutePath(), segFile.exists());
    File pf = profileFile("fastbike");
    Assume.assumeTrue("profile present", pf != null);

    List<OsmNodeNamed> wplist = new ArrayList<>();
    { OsmNodeNamed n = new OsmNodeNamed(); n.name = "from"; n.ilon = 187591431; n.ilat = 137562873; wplist.add(n); }
    { OsmNodeNamed n = new OsmNodeNamed(); n.name = "via1"; n.ilon = 187621400; n.ilat = 137610833; wplist.add(n); }
    { OsmNodeNamed n = new OsmNodeNamed(); n.name = "via2"; n.ilon = 187600585; n.ilat = 137690483; wplist.add(n); }
    { OsmNodeNamed n = new OsmNodeNamed(); n.name = "via3"; n.ilon = 187676265; n.ilat = 137716069; wplist.add(n); }
    { OsmNodeNamed n = new OsmNodeNamed(); n.name = "via4"; n.ilon = 187740949; n.ilat = 137668124; wplist.add(n); }
    { OsmNodeNamed n = new OsmNodeNamed(); n.name = "to"; n.ilon = 187651151; n.ilat = 137603038; wplist.add(n); }

    RoutingContext rctx = new RoutingContext();
    rctx.localFunction = pf.getAbsolutePath();
    RoutingEngine re = new RoutingEngine(null, null, segDir, wplist, rctx);
    re.quite = true;
    re.doRun(0);

    String err = re.getErrorMessage();
    OsmTrack track = re.getFoundTrack();
    System.out.println("=== replay_lunch_302709329_03_ROAD_60_hilly_GER_Scheideck_1_ ===");
    System.out.println("  error: " + (err == null ? "PASSED" : err));
    if (track != null && track.nodes != null) {
      System.out.println("  route: " + track.distance + "m / " + track.nodes.size() + " nodes");
      HostileStretch hs = RoundTripQualityGate.worstHostileStretchPaved(track);
      if (hs.isPresent()) {
        System.out.printf("  worst-hostile: %dm at (%.6f, %.6f) tags: %s%n",
          hs.meters, hs.startLat(), hs.startLon(), hs.startTags);
      } else {
        System.out.println("  hostile: none");
      }
      Assert.assertNull("expected clean point-to-point route through Komoot waypoints: " + err, err);
      Assert.assertTrue("expected no hostile stretch over the gate cap",
        !hs.isPresent() || hs.meters <= RoundTripQualityGate.MAX_CONTIGUOUS_HOSTILE_METERS);
    } else {
      Assert.fail("brouter returned no track: " + err);
    }
  }

  /**
   * Replay of "lunch__302709329__#03 ROAD 60 hilly GER Scheideck" — feeds the
   * sampled waypoints to brouter and reports whether the resulting
   * point-to-point route is clean enough to satisfy the round-trip
   * quality gate. Failure means the routing engine (with the
   * fastbike profile) can't reproduce the known-good route
   * even when given the right waypoints.
   */
  @Test
  public void replay_lunch_302709329_03_ROAD_60_hilly_GER_Scheideck() throws Exception {
    Assume.assumeTrue("loop-tests opt-in", Boolean.getBoolean("loop.tests"));
    File segDir = new File("../segments4");
    if (!segDir.exists()) segDir = new File("segments4");
    File segFile = new File(segDir, "E5_N45.rd5");
    Assume.assumeTrue("segment present at " + segFile.getAbsolutePath(), segFile.exists());
    File pf = profileFile("fastbike");
    Assume.assumeTrue("profile present", pf != null);

    List<OsmNodeNamed> wplist = new ArrayList<>();
    { OsmNodeNamed n = new OsmNodeNamed(); n.name = "from"; n.ilon = 187591431; n.ilat = 137562873; wplist.add(n); }
    { OsmNodeNamed n = new OsmNodeNamed(); n.name = "via1"; n.ilon = 
wplist.add(n); }
    { OsmNodeNamed n = new OsmNodeNamed(); n.name = "via3"; n.ilon = 187768555; n.ilat = 137518292; wplist.add(n); }
    { OsmNodeNamed n = new OsmNodeNamed(); n.name = "via4"; n.ilon = 187694087; n.ilat = 137503759; wplist.add(n); }
    { OsmNodeNamed n = new OsmNodeNamed(); n.name = "to"; n.ilon = 187666807; n.ilat = 137533775; wplist.add(n); }

    RoutingContext rctx = new RoutingContext();
    rctx.localFunction = pf.getAbsolutePath();
    RoutingEngine re = new RoutingEngine(null, null, segDir, wplist, rctx);
    re.quite = true;
    re.doRun(0);

    String err = re.getErrorMessage();
    OsmTrack track = re.getFoundTrack();
    System.out.println("=== replay_lunch_610276122_23_GRVL_55_mountainous_SUI_Olsberg ===");
    System.out.println("  error: " + (err == null ? "PASSED" : err));
    if (track != null && track.nodes != null) {
      System.out.println("  route: " + track.distance + "m / " + track.nodes.size() + " nodes");
      HostileStretch hs = RoundTripQualityGate.worstHostileStretchPaved(track);
      if (hs.isPresent()) {
        System.out.printf("  worst-hostile: %dm at (%.6f, %.6f) tags: %s%n",
          hs.meters, hs.startLat(), hs.startLon(), hs.startTags);
      } else {
        System.out.println("  hostile: none");
      }
      Assert.assertNull("expected clean point-to-point route through Komoot waypoints: " + err, err);
      Assert.assertTrue("expected no hostile stretch over the gate cap",
        !hs.isPresent() || hs.meters <= RoundTripQualityGate.MAX_CONTIGUOUS_HOSTILE_METERS);
    } else {
      Assert.fail("brouter returned no track: " + err);
    }
  }

  /**
   * Replay of "lunch__610281475__#22 GRVL 36 hilly SUI Leymen" — feeds the
   * sampled waypoints to brouter and reports whether the resulting
   * point-to-point route is clean enough to satisfy the round-trip
   * quality gate. Failure means the routing engine (with the
   * fastbike profile) can't reproduce the known-good route
   * even when given the right waypoints.
   */
  @Test
  public void replay_lunch_610281475_22_GRVL_36_hilly_SUI_Leymen() throws Exception {
    Assume.assumeTrue("loop-tests opt-in", Boolean.getBoolean("loop.tests"));
    File segDir = new File("../segments4");
    if (!segDir.exists()) segDir = new File("segments4");
    File segFile = new File(segDir, "E5_N45.rd5");
    Assume.assumeTrue("segment present at " + segFile.getAbsolutePath(), segFile.exists());
    File pf = profileFile("fastbike");
    Assume.assumeTrue("profile present", pf != null);

    List<OsmNodeNamed> wplist = new ArrayList<>();
    { OsmNodeNamed n = new OsmNodeNamed(); n.name = "from"; n.ilon = 187591524; n.ilat = 137562900; wplist.add(n); }
    { OsmNodeNamed n = new OsmNodeNamed(); n.name = "via1"; n.ilon = 187585672; n.ilat = 137539994; wplist.add(n); }
    { OsmNodeNamed n = new OsmNodeNamed(); n.name = "via2"; n.ilon = 187566567; n.ilat = 137491019; wplist.add(n); }
    { OsmNodeNamed n = new OsmNodeNamed(); n.name = "via3"; n.ilon = 187502028; n.ilat = 137489063; wplist.add(n); }
    { OsmNodeNamed n = new OsmNodeNamed(); n.name = "via4"; n.ilon = 187517208; n.ilat = 137533848; wplist.add(n); }
    { OsmNodeNamed n = new OsmNodeNamed(); n.name = "to"; n.ilon = 187565843; n.ilat = 137547170; wplist.add(n); }

    RoutingContext rctx = new RoutingContext();
    rctx.localFunction = pf.getAbsolutePath();
    RoutingEngine re = new RoutingEngine(null, null, segDir, wplist, rctx);
    re.quite = true;
    re.doRun(0);

    String err = re.getErrorMessage();
    OsmTrack track = re.getFoundTrack();
    System.out.println("=== replay_lunch_610281475_22_GRVL_36_hilly_SUI_Leymen ===");
    System.out.println("  error: " + (err == null ? "PASSED" : err));
    if (track != null && track.nodes != null) {
      System.out.println("  route: " + track.distance + "m / " + track.nodes.size() + " nodes");
      HostileStretch hs = RoundTripQualityGate.worstHostileStretchPaved(track);
      if (hs.isPresent()) {
        System.out.printf("  worst-hostile: %dm at (%.6f, %.6f) tags: %s%n",
          hs.meters, hs.startLat(), hs.startLon(), hs.startTags);
      } else {
        System.out.println("  hostile: none");
      }
      Assert.assertNull("expected clean point-to-point route through Komoot waypoints: " + err, err);
      Assert.assertTrue("expected no hostile stretch over the gate cap",
        !hs.isPresent() || hs.meters <= RoundTripQualityGate.MAX_CONTIGUOUS_HOSTILE_METERS);
    } else {
      Assert.fail("brouter returned no track: " + err);
    }
  }

  /**
   * Replay of "shorty__435539865__#01 ALRD 25 hilly SUI Ruehrberg" — feeds the
   * sampled waypoints to brouter and reports whether the resulting
   * point-to-point route is clean enough to satisfy the round-trip
   * quality gate. Failure means the routing engine (with the
   * fastbike profile) can't reproduce the known-good route
   * even when given the right waypoints.
   */
  @Test
  public void replay_shorty_435539865_01_ALRD_25_hilly_SUI_Ruehrberg() throws Exception {
    Assume.assumeTrue("loop-tests opt-in", Boolean.getBoolean("loop.tests"));
    File segDir = new File("../segments4");
    if (!segDir.exists()) segDir = new File("segments4");
    File segFile = new File(segDir, "E5_N45.rd5");
    Assume.assumeTrue("segment present at " + segFile.getAbsolutePath(), segFile.exists());
    File pf = profileFile("fastbike");
    Assume.assumeTrue("profile present", pf != null);

    List<OsmNodeNamed> wplist = new ArrayList<>();
    { OsmNodeNamed n = new OsmNodeNamed(); n.name = "from"; n.ilon = 187591522; n.ilat = 137562731; wplist.add(n); }
    { OsmNodeNamed n = new OsmNodeNamed(); n.name = "via1"; n.ilon = 187627246; n.ilat = 137562660; wplist.add(n); }
    { OsmNodeNamed n = new OsmNodeNamed(); n.name = "via2"; n.ilon = 187651497; n.ilat = 137560578; wplist.add(n); }
    { OsmNodeNamed n = new OsmNodeNamed(); n.name = "via3"; n.ilon = 187693913; n.ilat = 137570999; wplist.add(n); }
    { OsmNodeNamed n = new OsmNodeNamed(); n.name = "via4"; n.ilon = 187672464; n.ilat = 137585619; wplist.add(n); }
    { OsmNodeNamed n = new OsmNodeNamed(); n.name = "to"; n.ilon = 187635387; n.ilat = 137583717; wplist.add(n); }

    RoutingContext rctx = new RoutingContext();
    rctx.localFunction = pf.getAbsolutePath();
    RoutingEngine re = new RoutingEngine(null, null, segDir, wplist, rctx);
    re.quite = true;
    re.doRun(0);

    String err = re.getErrorMessage();
    OsmTrack track = re.getFoundTrack();
    System.out.println("=== replay_shorty_435539865_01_ALRD_25_hilly_SUI_Ruehrberg ===");
    System.out.println("  error: " + (err == null ? "PASSED" : err));
    if (track != null && track.nodes != null) {
      System.out.println("  route: " + track.distance + "m / " + track.nodes.size() + " nodes");
      HostileStretch hs = RoundTripQualityGate.worstHostileStretchPaved(track);
      if (hs.isPresent()) {
        System.out.printf("  worst-hostile: %dm at (%.6f, %.6f) tags: %s%n",
          hs.meters, hs.startLat(), hs.startLon(), hs.startTags);
      } else {
        System.out.println("  hostile: none");
      }
      Assert.assertNull("expected clean point-to-point route through Komoot waypoints: " + err, err);
      Assert.assertTrue("expected no hostile stretch over the gate cap",
        !hs.isPresent() || hs.meters <= RoundTripQualityGate.MAX_CONTIGUOUS_HOSTILE_METERS);
    } else {
      Assert.fail("brouter returned no track: " + err);
    }
  }

  /**
   * Replay of "shorty__435556037__#02 ALRD 32 hilly SUI Aesch" — feeds the
   * sampled waypoints to brouter and reports whether the resulting
   * point-to-point route is clean enough to satisfy the round-trip
   * quality gate. Failure means the routing engine (with the
   * fastbike profile) can't reproduce the known-good route
   * even when given the right waypoints.
   */
  @Test
  public void replay_shorty_435556037_02_ALRD_32_hilly_SUI_Aesch() throws Exception {
    Assume.assumeTrue("loop-tests opt-in", Boolean.getBoolean("loop.tests"));
    File segDir = new File("../segments4");
    if (!segDir.exists()) segDir = new File("segments4");
    File segFile = new File(segDir, "E5_N45.rd5");
    Assume.assumeTrue("segment present at " + segFile.getAbsolutePath(), segFile.exists());
    File pf = profileFile("fastbike");
    Assume.assumeTrue("profile present", pf != null);

    List<OsmNodeNamed> wplist = new ArrayList<>();
    { OsmNodeNamed n = new OsmNodeNamed(); n.name = "from"; n.ilon = 187591524; n.ilat = 137562900; wplist.add(n); }
    { OsmNodeNamed n = new OsmNodeNamed(); n.name = "via1"; n.ilon = 187623534; n.ilat = 137550004; wplist.add(n); }
    { OsmNodeNamed n = new OsmNodeNamed(); n.name = "via2"; n.ilon = 187607150; n.ilat = 137501292; wplist.add(n); }
    { OsmNodeNamed n = new OsmNodeNamed(); n.name = "via3"; n.ilon = 187596545; n.ilat = 137467509; wplist.add(n); }
    { OsmNodeNamed n = new OsmNodeNamed(); n.name = "via4"; n.ilon = 187557355; n.ilat = 137499577; wplist.add(n); }
    { OsmNodeNamed n = new OsmNodeNamed(); n.name = "to"; n.ilon = 187571138; n.ilat = 137533257; wplist.add(n); }

    RoutingContext rctx = new RoutingContext();
    rctx.localFunction = pf.getAbsolutePath();
    RoutingEngine re = new RoutingEngine(null, null, segDir, wplist, rctx);
    re.quite = true;
    re.doRun(0);

    String err = re.getErrorMessage();
    OsmTrack track = re.getFoundTrack();
    System.out.println("=== replay_shorty_435556037_02_ALRD_32_hilly_SUI_Aesch ===");
    System.out.println("  error: " + (err == null ? "PASSED" : err));
    if (track != null && track.nodes != null) {
      System.out.println("  route: " + track.distance + "m / " + track.nodes.size() + " nodes");
      HostileStretch hs = RoundTripQualityGate.worstHostileStretchPaved(track);
      if (hs.isPresent()) {
        System.out.printf("  worst-hostile: %dm at (%.6f, %.6f) tags: %s%n",
          hs.meters, hs.startLat(), hs.startLon(), hs.startTags);
      } else {
        System.out.println("  hostile: none");
      }
      Assert.assertNull("expected clean point-to-point route through Komoot waypoints: " + err, err);
      Assert.assertTrue("expected no hostile stretch over the gate cap",
        !hs.isPresent() || hs.meters <= RoundTripQualityGate.MAX_CONTIGUOUS_HOSTILE_METERS);
    } else {
      Assert.fail("brouter returned no track: " + err);
    }
  }

  /**
   * Replay of "shorty__435581815__#03 ROAD 34 flat GER Steinen" — feeds the
   * sampled waypoints to brouter and reports whether the resulting
   * point-to-point route is clean enough to satisfy the round-trip
   * quality gate. Failure means the routing engine (with the
   * fastbike profile) can't reproduce the known-good route
   * even when given the right waypoints.
   */
  @Test
  public void replay_shorty_435581815_03_ROAD_34_flat_GER_Steinen() throws Exception {
    Assume.assumeTrue("loop-tests opt-in", Boolean.getBoolean("loop.tests"));
    File segDir = new File("../segments4");
    if (!segDir.exists()) segDir = new File("segments4");
    File segFile = new File(segDir, "E5_N45.rd5");
    Assume.assumeTrue("segment present at " + segFile.getAbsolutePath(), segFile.exists());
    File pf = profileFile("fastbike");
    Assume.assumeTrue("profile present", pf != null);

    List<OsmNodeNamed> wplist = new ArrayList<>();
    { OsmNodeNamed n = new OsmNodeNamed(); n.name = "from"; n.ilon = 187591431; n.ilat = 137562873; wplist.add(n); }
    { OsmNodeNamed n = new OsmNodeNamed(); n.name = "via1"; n.ilon = 187637873; n.ilat = 137586552; wplist.add(n); }
    { OsmNodeNamed n = new OsmNodeNamed(); n.name = "via2"; n.ilon = 187687238; n.ilat = 137636414; wplist.add(n); }
    { OsmNodeNamed n = new OsmNodeNamed(); n.name = "via3"; n.ilon = 187737769; n.ilat = 137647144; wplist.add(n); }
    { OsmNodeNamed n = new OsmNodeNamed(); n.name = "via4"; n.ilon = 187670463; n.ilat = 137634627; wplist.add(n); }
    { OsmNodeNamed n = new OsmNodeNamed(); n.name = "to"; n.ilon = 187637039; n.ilat = 137585085; wplist.add(n); }

    RoutingContext rctx = new RoutingContext();
    rctx.localFunction = pf.getAbsolutePath();
    RoutingEngine re = new RoutingEngine(null, null, segDir, wplist, rctx);
    re.quite = true;
    re.doRun(0);

    String err = re.getErrorMessage();
    OsmTrack track = re.getFoundTrack();
    System.out.println("=== replay_shorty_435581815_03_ROAD_34_flat_GER_Steinen ===");
    System.out.println("  error: " + (err == null ? "PASSED" : err));
    if (track != null && track.nodes != null) {
      System.out.println("  route: " + track.distance + "m / " + track.nodes.size() + " nodes");
      HostileStretch hs = RoundTripQualityGate.worstHostileStretchPaved(track);
      if (hs.isPresent()) {
        System.out.printf("  worst-hostile: %dm at (%.6f, %.6f) tags: %s%n",
          hs.meters, hs.startLat(), hs.startLon(), hs.startTags);
      } else {
        System.out.println("  hostile: none");
      }
      Assert.assertNull("expected clean point-to-point route through Komoot waypoints: " + err, err);
      Assert.assertTrue("expected no hostile stretch over the gate cap",
        !hs.isPresent() || hs.meters <= RoundTripQualityGate.MAX_CONTIGUOUS_HOSTILE_METERS);
    } else {
      Assert.fail("brouter returned no track: " + err);
    }
  }


}
